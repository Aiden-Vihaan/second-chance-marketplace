describe("Wishlist", () => {
  test("Add to wishlist endpoint exists", () => {
    expect(true).toBe(true);
  });

  test("Remove from wishlist endpoint exists", () => {
    expect(true).toBe(true);
  });
});
